
export VENDOR="${VENDOR:-lge}"
export DEVICE="${DEVICE:-hammerhead}"

export MER_ROOT="${MER_ROOT:-/home/travis/NEXUS5/mer-July-t31}"
export ANDROID_ROOT="${ANDROID_ROOT:-/home/travis/NEXUS5/ubu-July-t31}"
export IMGDEST="${IMGDEST:-/home/travis/nexus5}"

# always aim for the latest:
export RELEASE="${RELEASE:-2.0.2.48}"
export EXTRA_STRING="${EXTRA_STRING:-beta4}"

export BRANCH="${BRANCH:-hybris-12.1}"
export JOBS="${JOBS:-4}"

export DHD_REPO="${DHD_REPO:-http://repo.merproject.org/obs/nemo:/devel:/hw:/lge:/hammerhead/sailfish_latest_armv7hl/}"
export MW_REPO="${MW_REPO:-http://repo.merproject.org/obs/nemo:/devel:/hw:/common/sailfish_latest_armv7hl/}"
export EXTRA_REPO="${EXTRA_REPO:-x}"
export TARGET="${TARGET:-2.0.1.11}"
export ARCH="${ARCH:-armv7hl}"


# printf "vars in use:
#     VENDOR=$VENDOR
#     DEVICE=$DEVICE
#
#     MER_ROOT=$MER_ROOT
#     ANDROID_ROOT=$ANDROID_ROOT
#     IMGDEST=$IMGDEST
#
#     RELEASE=$RELEASE
#     EXTRA_STRING=$EXTRA_STRING
#
#     BRANCH=$BRANCH
#     JOBS=$JOBS
#
#     DHD_REPO=$DHD_REPO
#     MW_REPO=$MW_REPO
#     EXTRA_REPO=$EXTRA_REPO
#
#     ARCH=$ARCH
#     TARGET=$TARGET
# "
